spring:
  database:
    driverClassName: org.postgresql.Driver
  datasource:
    plateform: postgres
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: passw0rd
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        formate_sql: false
        default_schema: adjustment
        jdbc:
          batch_size: 50
    show-sql: false

